//---------------------------------------------------------------------------

#ifndef LinearfunktionH
#define LinearfunktionH

#include "verketteteListe.h"

class Linearfunktion
{
public:
        //Konstruktor + Destruktor
        Linearfunktion( int anzahlKoeffizienten );
        virtual ~Linearfunktion();

        //Methode zur Bestimmmung der Anzahl der Koeffizienten
        int getAnzahlKoeffizienten();

        //Methode zum Setzen der Koeffizienten
        bool setKoeffizient( double dWert );

private:
        int aAnzahlKoeffizienten;
        Liste aKoeffizientenListe;
        double aLoesung;
};



//---------------------------------------------------------------------------
#endif
 